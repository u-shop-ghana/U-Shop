{
  "name": "ushop",
  "version": "0.1.0",
  "private": true,
  "description": "U-Shop — Ghana's trusted campus tech marketplace",
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "typecheck": "turbo typecheck",
    "clean": "turbo clean",
    "format": "prettier --write \"**/*.{ts,tsx,js,jsx,json,md}\"",
    "test": "turbo test"
  },
  "packageManager": "pnpm@9.15.0",
  "engines": {
    "node": ">=20.0.0"
  },
  "devDependencies": {
    "eslint": "^10.1.0",
    "prettier": "^3.8.1",
    "turbo": "^2.8.20"
  },
  "dependencies": {
    "@sentry/nextjs": "10"
  }
}
